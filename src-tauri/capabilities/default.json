{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for windows",
  "windows": ["main", "cleaner"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
